# `@scrambling/shared-types`

Shared TypeScript types for cross-package contracts.

Includes internal API DTOs plus normalized external-course DTOs used by the web app.

## Commands

```bash
npm install --prefix packages/shared-types
npm run typecheck --prefix packages/shared-types
```
